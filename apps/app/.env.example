# Supabase Configuration
# Get these values from your Supabase project settings (https://app.supabase.com)

# Public Supabase URL (Settings > API > Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Public Anon Key (Settings > API > Project API keys > anon public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Server-only Service Role Key (Settings > API > Project API keys > service_role)
# ⚠️ NEVER expose this key to the browser or commit it to version control
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT Secret for token verification (Settings > API > JWT Settings > JWT Secret)
# Used for validating Supabase JWT tokens
SUPABASE_JWT_SECRET=your-jwt-secret-here

# Environment
NODE_ENV=development

# Optional: Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
